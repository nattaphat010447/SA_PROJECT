services:
  # 1. Database Service (PostgreSQL)
  db:
    image: postgres:15-alpine
    container_name: squader-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: gamematch_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./server/src/init.sql:/docker-entrypoint-initdb.d/init.sql 

  # 2. Backend Service (Node.js)
  server:
    build: ./server
    container_name: squader-server
    ports:
      - "3000:3000"
    volumes:
      - ./server:/app
      - /app/node_modules
    env_file:
      - ./server/.env
    environment:
      - DATABASE_URL=postgres://postgres:password123@db:5432/gamematch_db
      - PORT=3000
    command: npm run dev
    depends_on:
      - db

  # 3. Frontend Service (Vue.js)
  client:
    build: ./client
    container_name: squader-client
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - VITE_API_URL=http://localhost:3000/api
    command: npm run dev -- --host 0.0.0.0
    depends_on:
      - server

  # 4. Admin Frontend Service (Vue.js)
  admin:
    build: ./admin
    container_name: squader-admin
    ports:
      - "5174:5174"
    volumes:
      - ./admin:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:3000/api
    depends_on:
      - server

volumes:
  pgdata: